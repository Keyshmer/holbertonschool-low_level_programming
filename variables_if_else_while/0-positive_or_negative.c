#include <stdlib.h>
#include <time.h>
/**
 * main - determine if n is negative or negative etc.
 *
 * Return: 0
/**/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	print(n);

	if (n > 0)
	{
		print (is positive0);
	}
	else
	{
		if (n == 0)
		{
			print (is zero);
		}
		else
		{
		if (n < 0)
		{
			print (is negative);
		}
	return (0);
}
