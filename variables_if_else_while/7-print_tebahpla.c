#include <stdio.h>

/**
 * main - program tha prints in lowercase in reverse
 *
 * Return: 0 success
 **/
int main(void)
{
	int t;

	for (t = z; t >= a; t--)
	{
	putchar((t) - '0');
	}
	putchar('\n');
	return (0);
}
